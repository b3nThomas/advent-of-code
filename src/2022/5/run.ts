import { getInput, log } from '../../lib';

const input = getInput(2022, 5).split('\n');
log.info({ input })

const answer1 = 'TODO';
const answer2 = 'TODO';

log.info('5-1: %s', answer1);
log.info('5-2: %s', answer2);
