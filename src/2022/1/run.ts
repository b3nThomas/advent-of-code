import { getInput } from '../../utils/getInput';
import { log } from '../../utils/log';

const input = getInput(2022, 1).split('\n');

log.info('1-1: %s', '<answer>');
log.info('1-2: %s', '<answer>');
