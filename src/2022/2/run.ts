import { getInput } from '../../utils/getInput';
import { log } from '../../utils/log';

const input = getInput(2022, 2).split('\n');

log.info('2-1: %s', '<answer>');
log.info('2-2: %s', '<answer>');
