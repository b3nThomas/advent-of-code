// import { getInput } from '../../utils/getInput';
// import { log } from '../../utils/log';

// const input = getInput(2021, <x>).split('\n').filter((row) => row);

// log.info('<x>-1: %s', <answer>);
// log.info('<x>-2: %s', <answer>);
